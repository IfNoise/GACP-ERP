{
  "name": "shared-keycloak",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "root": "libs/shared/keycloak",
  "sourceRoot": "libs/shared/keycloak/src",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --project libs/shared/keycloak/tsconfig.json --noEmit"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/shared/keycloak/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/libs/shared/keycloak"],
      "options": {
        "jestConfig": "libs/shared/keycloak/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": ["type:utility", "scope:shared", "domain:auth"]
}
