server:
  port: 8080

log:
  level: info
  format: text

# Use postgres superuser for schema creation
database:
  postgres:
    host: postgres
    port: 5432
    database: zitadel
    user: gacp
    password: gacp_secret_2026
    max_open_conns: 25
    max_idle_conns: 5
    max_lifetime: 30m
    max_idle_time: 5m
    ssl_mode: disable

eventstore:
  postgres:
    host: postgres
    port: 5432
    database: zitadel
    user: gacp
    password: gacp_secret_2026
    max_open_conns: 25
    max_idle_conns: 5
    max_lifetime: 30m
    max_idle_time: 5m
    ssl_mode: disable

domain: localhost:8080
external_port: 8080
external_secure: false

# Initialize first admin user
firstInstance:
  org:
    name: GACP-ERP
  iamAdminUser:
    username: zitadel-admin
    firstName: ZITADEL
    lastName: Admin
    email: zitadel-admin@zitadel.localhost
    password: ZitadelAdmin123!@#
