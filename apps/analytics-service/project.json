{
  "name": "analytics-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/analytics-service/src",
  "projectType": "application",
  "tags": ["type:app", "runtime:nestjs", "domain:analytics", "compliance:critical"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/analytics-service",
        "main": "apps/analytics-service/src/main.ts",
        "tsConfig": "apps/analytics-service/tsconfig.app.json"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node -r ts-node/register/transpile-only -r tsconfig-paths/register apps/analytics-service/src/main.ts",
        "cwd": "{workspaceRoot}",
        "envFile": "docker/.env"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "apps/analytics-service"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": { "lintFilePatterns": ["apps/analytics-service/**/*.ts"] }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/apps/analytics-service"],
      "options": { "jestConfig": "apps/analytics-service/jest.config.ts" }
    }
  }
}
